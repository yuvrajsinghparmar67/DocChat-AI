# Optional. Leave unset to use Vite's dev proxy (see vite.config.js) or a
# same-origin production deployment. Set this only if the frontend and
# backend are deployed to different origins.
VITE_API_BASE_URL=
